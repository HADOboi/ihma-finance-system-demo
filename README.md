# IHMA Finance System Demo (plain Vite SPA)

```sh
npm install
npm run dev      # http://localhost:5173
npm run build    # outputs to dist/
```

## Vercel settings
- Framework Preset: **Vite**
- Build Command: `npm run build`
- Output Directory: `dist`
- No vercel.json needed.
